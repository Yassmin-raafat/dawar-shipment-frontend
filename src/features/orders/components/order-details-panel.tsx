import type { Order } from "@/features/orders/types/order";
import Image from "next/image";

export default function OrderDetailsPanel({ order, t }: { order: Order; t: (key: string, values?: Record<string, string | number>) => string }) {
  return <section className="rounded-2xl border border-border/40 bg-card p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] text-text-secondary">{t("orderDetails")}</p><h2 className="mt-1 text-lg font-semibold">{order.trackingId}</h2></div><span className="rounded-full bg-primary-muted px-3 py-1 text-[10px] font-medium text-primary">{t(`status.${order.status}`)}</span></div><dl className="mt-6 grid grid-cols-2 gap-4 text-xs"><div><dt className="text-[10px] text-text-secondary">{t("recipient")}</dt><dd className="mt-1 font-medium">{order.recipient.name}</dd></div><div><dt className="text-[10px] text-text-secondary">{t("driver")}</dt><dd className="mt-1 font-medium">{order.driver.name}</dd></div><div><dt className="text-[10px] text-text-secondary">{t("pickup")}</dt><dd className="mt-1">{order.origin}</dd></div><div><dt className="text-[10px] text-text-secondary">{t("delivery")}</dt><dd className="mt-1">{order.destination}</dd></div></dl><div className="mt-6 border-t border-border/40 pt-4 text-xs"><div className="flex justify-between py-1"><span className="text-text-secondary">{t("baseFee")}</span><span>{order.fees.base}</span></div><div className="flex justify-between py-1"><span className="text-text-secondary">{t("tax")}</span><span>{order.fees.tax}</span></div><div className="mt-2 flex justify-between border-t border-border/40 pt-3 font-semibold"><span>{t("totalFee")}</span><span className="text-primary">{order.fees.total}</span></div></div><div className="mt-5 rounded-xl bg-secondary p-3 text-xs"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] text-text-secondary">{t("driverDetails")}</p><p className="mt-1 font-medium">{order.driver.name} · {order.driver.role}</p><p className="mt-1 text-text-secondary">{order.driver.id} · {order.driver.vehicle}</p></div>{order.driver.vehicleImage && <Image src={order.driver.vehicleImage} alt={order.driver.vehicle} width={104} height={64} className="h-16 w-24 rounded-lg object-cover" />}</div></div></section>;
}

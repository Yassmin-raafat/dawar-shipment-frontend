import type { Order } from "@/features/orders/types/order";

export default function OrderCard({ order, selected, onSelect, labels }: { order: Order; selected: boolean; onSelect: () => void; labels: Record<string, string> }) {
  return <button type="button" onClick={onSelect} aria-pressed={selected} className={`w-full rounded-2xl border p-3 text-start transition ${selected ? "border-primary bg-primary-muted/50" : "border-border/40 bg-card hover:bg-secondary"}`}><div className="flex items-start justify-between gap-2"><span className="flex min-w-0 items-center gap-2 text-[11px] font-semibold text-text-primary"><span className="grid size-6 shrink-0 place-items-center rounded-lg bg-primary-muted text-primary">⌁</span><span className="truncate">{order.trackingId}</span></span><span className="shrink-0 rounded-full bg-secondary px-2 py-1 text-[8px] font-medium text-text-secondary">{labels[order.status]}</span></div><div className="mt-3 grid grid-cols-[1fr_auto_1fr] items-start gap-2 text-[9px]"><div className="min-w-0"><p className="truncate font-medium">{order.origin}</p><p className="mt-1 text-text-secondary">{order.pickupDate}</p></div><div className="pt-1 text-text-muted">→</div><div className="min-w-0 text-end"><p className="truncate font-medium">{order.destination}</p><p className="mt-1 text-text-secondary">{order.deliveryDate}</p></div></div><div className="mt-2 h-1 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-primary" style={{ width: `${order.progress}%` }} /></div><div className="mt-3 flex items-center gap-2 text-[9px] text-text-secondary"><span className="grid size-6 shrink-0 place-items-center rounded-full bg-primary-muted text-[8px] font-semibold text-primary">{order.driver.initials}</span><span className="min-w-0 flex-1 truncate"><b className="font-medium text-text-primary">{order.driver.name}</b><span className="ms-1">{order.driver.role}</span></span><span aria-label={order.recipient.phone} className="grid size-6 place-items-center rounded-lg border border-border/50 text-text-muted">⌕</span></div></button>;
}

import DriversPage from "@/features/drivers/components/drivers-page";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Drivers | Dawar Parcel" };

export default function DriversRoutePage() {
  return <DriversPage />;
}
